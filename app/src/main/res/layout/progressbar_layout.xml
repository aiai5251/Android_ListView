<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical">


    <!--android:indeterminate="true" ,  true 为不精确显示进度，false为精确显示-->

    <ProgressBar

        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar2" />

    <ProgressBar
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar3" />

    <!-- max 最大进度，progress：当前第一进度 secondaryProgress：第二进度-->

    <!--修改自己进度条的样式  android:progressDrawable=""-->
    <!--style="?android:attr/progressBarStyleHorizontal"-->
    <!-- 自定义设置progressBar的样式：  android:progressDrawable="@drawable/progress_bar" （style="@android:style/Widget.ProgressBar.Horizontal"）-->

    <ProgressBar
        android:max="100"
        android:progress="50"
        android:secondaryProgress="80"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:progressDrawable="@drawable/progress_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar_horizontal"
        android:layout_gravity="center_horizontal" />

    <Button
        android:id="@+id/add"
        android:text="@string/add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/reduce"
        android:text="@string/reduce"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/reset"
        android:text="@string/reset"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/progressDialog"
        android:text="显示进度条对话框"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</LinearLayout>
